{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "comment": "SPA fallback so client-side routes (/plan, /cards, /grow, /checks) work on refresh. Set VITE_API_BASE to the deployed backend URL in Vercel project settings, e.g. https://cogprint-api.onrender.com"
}
