fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
numpy>=1.26.0
python-dotenv>=1.0.0

# Optional — only needed for the deprecated narrative mode in
# agents/performance_optimizer.py. The live pipeline is fully API-free.
anthropic>=0.40.0

# Dev / test only
pytest>=8.0.0
httpx>=0.27.0
